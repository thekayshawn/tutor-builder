import * as React from "react";
import { EditorInterfaceState } from "../EditorTypes";

export default function EditorInterfaceView({
  pageContent,
}: {
  pageContent: EditorInterfaceState["pageContent"];
}) {
  return <div>EditorInterfaceView</div>;
}
